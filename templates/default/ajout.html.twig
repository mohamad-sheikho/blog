{% extends 'base.html.twig' %} 

{% block title %}Ajouter un article{% endblock %}

{% block body %}
    {% if not form.vars.valid %}

          {{ form_start(form, {'attr':{'class':'was-validated'}}) }}
          {% else  %} 
                {{ form_start(form)}}
    {% endif %}
          {{ form_label(form.titre, null, {'labler_attr': {'class':'form-label' }} ) }}
          {{ form_widget(form.titre, {'attr': {'class': 'form-control'}} ) }}
    
        <div id="validationServer03Feedback" class="invalid-feedback">
            
          
          {{ form_errors(form.titre) }}
        </div>

    {{ form_label(form.contenu, null, {'labler_attr': {'class':'form-label' }} ) }}
    {{ form_widget(form.contenu, {'attr': {'class': 'form-control'}})}}

    {{ form_label(form.dateCreation, null, {'labler_attr': {'class':'form-label' }} ) }}
    {{ form_widget(form.dateCreation, {'attr': {'class': 'form-control'}} ) }}
 

     {{ form_label(form.categories, null, {'labler_attr': {'class':'form-label' }} ) }}
    {{ form_widget(form.categories, {'attr': {'class': 'form-control'}} ) }}
    {{ form_widget(form.brouillon,{'attr' : {'class': 'btn btn-default'}}) }}
		{{ form_widget(form.publier,{'attr' : {'class': 'btn btn-success'}}) }}

        {{ form_rest(form) }}
        {# <button type="submit" class="btn btn-success  mt-3 mb-3">Enregistrer</button> #}
    {{ form_end(form) }}

{% endblock %}